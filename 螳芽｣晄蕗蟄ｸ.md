# GRACE Trade Ledger — 安裝教學

這份系統包含三個部分：資料庫（PostgreSQL）、後端伺服器（Node.js）、網頁前端（也是 Node.js）。全部都要先安裝好，才能在瀏覽器打開系統。跟著下面步驟做，全程用「終端機」貼指令執行即可。

---

## 第 0 步：解壓縮

把收到的 `grace-trade-ledger.zip` 解壓縮到桌面（雙擊，或右鍵選「解壓縮全部」），會得到一個資料夾，裡面有 `server`、`web`、`README.md` 這些東西。記住這個資料夾的路徑，等一下會用到。

---

## 第 1 步：安裝 Node.js

到 https://nodejs.org 下載並安裝 **LTS 版本**（綠色按鈕那個），跟一般軟體一樣點「下一步」裝完就好。

裝完後打開終端機（Mac：按 `⌘+空白鍵`，輸入「terminal」；Windows：搜尋「PowerShell」），輸入：

```bash
node -v
```

有印出版本號（例如 `v20.x.x`）就是裝好了。

---

## 第 2 步：安裝 PostgreSQL（資料庫）

**Mac：**
1. 先裝 Homebrew（如果還沒裝過）：到 https://brew.sh 複製首頁那行安裝指令，貼到終端機執行。
2. 再執行：
```bash
brew install postgresql@16
brew services start postgresql@16
createdb grace_erp
```

**Windows：**
1. 到 https://www.postgresql.org/download/windows/ 下載安裝檔，安裝過程會要你設一組密碼（安裝時的預設帳號是 `postgres`），**記住這組密碼**。
2. 裝完後打開「SQL Shell (psql)」（安裝時會一起裝好，在開始選單找得到），一路按 Enter 用預設值，最後輸入你剛剛設的密碼登入，再輸入：
```sql
CREATE DATABASE grace_erp;
```

---

## 第 3 步：設定資料庫連線

用終端機進入解壓縮出來的資料夾裡的 `server` 子資料夾（把下面路徑換成你實際解壓縮的位置）：

```bash
cd 桌面/grace-trade-ledger/server
```

把範例設定檔複製一份：

```bash
cp .env.example .env
```

**Windows 使用者**：如果你在第 2 步有設密碼，要打開 `server` 資料夾裡新出現的 `.env` 檔案（用記事本開就好），把裡面的
`postgresql://postgres:postgres@localhost:5432/grace_erp?schema=public`
改成
`postgresql://postgres:你剛剛設的密碼@localhost:5432/grace_erp?schema=public`
存檔。

**Mac 使用者**：這步通常不用改，直接跳過。

---

## 第 4 步：安裝套件、建立資料表、灌入範例資料

還是在 `server` 資料夾裡，依序執行（每行貼上去按 Enter，等它跑完再貼下一行）：

```bash
npm install
npx drizzle-kit migrate
npm run seed
```

跑完 `npm run seed` 應該會看到類似這樣的輸出：
```
PINE-001: Landed Unit Cost = ¥615.00
Quotation QT-2026-00001: qty 300 @ ¥800 -> profit ¥55500, margin 23.1%
Seed complete.
```
看到 `Seed complete.` 就代表資料庫準備好了。

---

## 第 5 步：啟動系統

**這一步要開兩個終端機視窗**（因為伺服器和網頁要同時跑著）。

**視窗 A**（還在 `server` 資料夾裡）：
```bash
npm run dev
```
看到 `GRACE Trade Ledger API listening on :4000` 就是成功了，這個視窗**不要關**，關掉系統就停了。

**視窗 B**（開一個新的終端機視窗）：
```bash
cd 桌面/grace-trade-ledger/web
npm install
npm run dev
```
看到類似 `Local: http://localhost:5173/` 就是成功了，這個視窗也**不要關**。

---

## 第 6 步：打開系統

打開瀏覽器（Chrome、Safari 都可以），網址列輸入：

```
http://localhost:5173
```

就會看到 GRACE Trade Ledger 的畫面了。側邊欄最下面可以切換中文／日本語。

---

## 下次要用怎麼開？

不用重新安裝，只要重複「第 5 步」跟「第 6 步」——開兩個終端機視窗，分別在 `server` 和 `web` 資料夾執行 `npm run dev`，再用瀏覽器打開 `http://localhost:5173` 就好。

## 卡住了怎麼辦？

如果哪一步出現紅字錯誤訊息，把錯誤訊息整段複製貼給我，我幫你看。最常見的狀況是 PostgreSQL 沒有真的啟動，或是 `.env` 裡的密碼跟你設定的不一樣。
